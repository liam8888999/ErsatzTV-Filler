const API_BASE = "/api/"
const CONFIG_ROUTE = API_BASE + "config/"
const CONFIG_ROUTE_EDIT = CONFIG_ROUTE + "edit"

const ROUTE_CONSTANTS = () => {
    return {
        CONFIG_ROUTE_EDIT
    }
}

module.exports = {
    ROUTE_CONSTANTS
}