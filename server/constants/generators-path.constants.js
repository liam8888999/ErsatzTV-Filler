const workdir = "server/workdir/";
const weatherdir = "server/weatherdir/";

const GENERATOR_CONSTANTS = () => {
    return {
        workdir, weatherdir
    }
}

module.exports = {
    GENERATOR_CONSTANTS,
};
